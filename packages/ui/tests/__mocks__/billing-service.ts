// Stub for @/services/BillingService used only for type imports
// in the dashboard components when tested from packages/ui vitest context.
// The AutoTopUpState type is re-declared inline in the components; this module
// only needs to exist (not export anything) to avoid module-not-found errors.
export {};
